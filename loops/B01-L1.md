# B01-L1 — Item Tags

**Backlog:** B01 (PRD §12) · **Loop:** L1 · **Date:** 2026-07-18
**Previous loop:** none

## Plan (post-grill)

Predefined occasion tags (not free-form) — decided in grill to avoid duplicating
the existing free-form `style` field. Fixed set, users multi-select.

- **Tag set** (const in `src/lib/wardrobe.ts`): `ITEM_TAGS = ['ทำงาน', 'ลำลอง', 'ออกเดท', 'งานทางการ', 'เที่ยว', 'ออกกำลังกาย']`. No tags table — YAGNI.
- **Migration** `010_item_tags.sql`: `alter table public.items add column if not exists tags text[] not null default '{}'` (additive, non-destructive).
- **Type + server fns** (`items.functions.ts` + `wardrobe.ts`): add `tags?: string[]` to `WardrobeItem`; wire through `mapRow` (`tags: row.tags ?? []`), `ItemInputSchema` + `saveItem` insert, and `updateItem` patch schema.
- **Input UX** — multi-select toggle chips from `ITEM_TAGS` in `UploadItem.tsx` and `EditItem.tsx`. `EMPTY_DRAFT.tags = []`; preserve selected tags when AI auto-fill replaces the draft (`{ ...result, tags: draft?.tags ?? [] }`). Satisfies AC1 (add/remove unlimited from the set).
- **Search** — extend the Wardrobe `filteredItems` predicate (`wardrobe.tsx`) to also match `item.tags`. Satisfies AC2 (search by tag). No filter chip row (grill decision).

## Implemented

- `supabase/migrations/010_item_tags.sql` — add `tags text[] not null default '{}'` to `items`
- `src/lib/wardrobe.ts` — `tags?: string[]` on `WardrobeItem`; exported `ITEM_TAGS` const (6 occasion tags)
- `src/lib/items.functions.ts` — `mapRow` returns `tags`; `tags` added to save/update zod schemas + `saveItem` insert
- `src/components/UploadItem.tsx` — "แท็ก" toggle-chip row; `EMPTY_DRAFT.tags=[]`; AI-fill preserves selected tags
- `src/components/EditItem.tsx` — "แท็ก" toggle-chip row (undefined tags treated as [])
- `src/routes/wardrobe.tsx` — search predicate also matches `item.tags`

## Gate

lint ✅ · build ✅ · migration ✅ · renders ✅

- lint: 0 errors, 6 pre-existing warnings (untouched `ui/*` files)
- build: exit 0, client+SSR built
- migration: `010_item_tags.sql` ran + done on `bun run dev` cold start (server on :5173)
- renders: GET /wardrobe → 200, full SSR (title/header/search/categories/empty-state)

## Scrutinize findings

_(Opus, fresh context; traced all persistence/UX seams — code confirmed correct end-to-end.)_

- **major** — PRD §3.6/§3.3 still said "free-form / unlimited," contradicting the grill-decided predefined set — **fixed here** (reconciled PRD wording; doc-only, no code change — remedy is not a code re-loop)
- nit — tags not displayed on `WardrobeCard`/grid, only in edit modals — **deferred** (AC needs only add/remove + search)
- nit — chip-picker block duplicated ~30 lines in `UploadItem` + `EditItem` — **deferred** (cosmetic; extract `<TagPicker/>` later)
- nit — `WardrobeItem.tags?` optional vs not-null DB column — **deferred** (harmless; all reads guard `?? []`)

## Decision

✅ done — gate green, code correct across the full round-trip, major (doc contradiction) fixed in place, nits deferred. Committed on `feat/b01-item-tags`.
