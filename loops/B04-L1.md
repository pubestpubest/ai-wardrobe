# B04-L1 — Profile optional body measurements

**Backlog:** B04 (PRD §12) · **Loop:** L1 · **Date:** 2026-07-19
**Previous loop:** none

## Plan (post-grill)

**Grilled → deferred, no code.** Decision (2026-07-19): don't build body-measurement
fields now. The only consumer is Virtual Try-On (B08), which is the last backlog
item and currently mocked — building measurement columns/UI before try-on defines
what it needs is speculative (YAGNI). height/weight already exist on `profiles`.

**Fold into B08:** when Virtual Try-On is built and specifies which measurements it
actually uses, add exactly those (optional, on the DB-backed `profiles` from B07a).

## Implemented

- (none — deferred)

## Gate

n/a — no code

## Scrutinize findings

- n/a

## Decision

⏭️ deferred → folded into B08 (Virtual Try-On). Not built; queue skips it.
