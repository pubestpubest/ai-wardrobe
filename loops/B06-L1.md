# B06-L1 — Affiliate Shopping UI Polish

**Backlog:** B06 (PRD §12) · **Loop:** L1 · **Date:** 2026-07-19
**Previous loop:** none

## Plan (post-grill)

Add category filter + store(brand) filter + search to the Discover page.
**Pure client-side** — the ~50-item affiliate catalog is a shared read-only
catalog already fully loaded via `useAffiliateProducts`; no migration, no
server change. Mirrors the Wardrobe page's search+chips pattern.

Grilled decisions:
- **Store filter = brand only** (the `store` field: Uniqlo/Zara/H&M/…), a **dropdown** with options derived from the catalog's distinct stores (sorted), single-select "ทั้งหมด" default. (No platform filter — grill dropped it.)
- **Search** = name + description, case-insensitive, space-stripped **substring** (Thai is spaceless). Mirror the wardrobe search input.
- **Category** = chips, wardrobe categories (`CATEGORY_LABELS`), single-select "ทั้งหมด" default (affiliate `category` == wardrobe category).
- **Combine** = category AND brand AND search, single-select each (`useMemo`).
- **Scope** = exactly these 3. No price sort, no platform, no formality (§12's "use B01 tag taxonomy" is moot — affiliate rows have category/style, not B01 tags).

- **`src/routes/discover.tsx`** (only file): add `search`/`category`/`store` state; `useMemo` filtered list (name/desc substring + category eq + store eq); render a search bar, a category chip row, a store `<select>` dropdown, and a "no results → clear filters" empty state. Reuse `Input` + the chip styling from `wardrobe.tsx`; keep the existing grid + `AffiliateItemModal`.

## Implemented

- `src/routes/discover.tsx` (only, 82→183 lines) — search Input (name+desc substring, space-stripped) + category chip row (`CATEGORY_LABELS`) + store `<select>` (distinct stores from catalog) + `useMemo` AND-combined filter; filtered grid; "ไม่พบ / ล้างตัวกรอง" empty state; header shows filtered count. No migration/server/other files.

## Gate

lint ✅ · build ✅ · migration ✅ (n/a — no migration) · renders ✅

- lint: 0 problems; build: exit 0; tsc: no new errors in discover.tsx
- renders: GET /discover → 200, filter UI SSRs (search `ค้นหา`, `ทั้งหมด` chip, store `<select>`), no SSR errors

## Scrutinize findings

_(Opus, fresh, correctness-weighted (low security surface). Verified: store options from FULL catalog (stable/deduped/sorted — the flagged risk did NOT occur); AND-combination correct; search space-strip+lowercase on query+targets, null desc guarded; empty/clear states can't strand the user; category ids match the data. Verdict: ship.)_

- nit — header count flashed "0 รายการ" during load — **fixed here** (`isLoading ? "กำลังโหลด…" : …`) — on-theme for a polish loop
- nit — selected store could show blank if the (static) catalog ever changed — **deferred** (unreachable with the read-only catalog)
- nit — category chip order differs slightly from Wardrobe — **deferred** (cosmetic)

## Decision

✅ done — gate green, scrutinize ship, load-flash nit fixed in place. Committed on `feat/b06-affiliate-filters` (re-branched off the B05 chain per the scrutinize process note).
